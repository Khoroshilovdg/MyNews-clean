---
const { title, date, excerpt, category } = Astro.props;
---

<article class="py-6 border-b border-gray-200">
  <h2 class="text-2xl font-serif font-bold mb-1 hover:underline cursor-pointer">{title}</h2>
  <p class="text-sm text-gray-500 mb-2">{date.toLocaleDateString("ru-RU")} · {category}</p>
  <p class="text-base leading-relaxed">{excerpt}</p>
</article>