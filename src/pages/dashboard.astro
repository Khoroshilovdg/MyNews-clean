---
// src/pages/dashboard.astro
import BaseLayout from "../layouts/BaseLayout.astro";
import { supabase } from "../lib/supabase";

// Проверяем авторизацию
const accessToken = Astro.cookies.get("sb-access-token");
const refreshToken = Astro.cookies.get("sb-refresh-token");

if (!accessToken || !refreshToken) {
  return Astro.redirect("/signin");
}

// Устанавливаем сессию и получаем пользователя
await supabase.auth.setSession({
  access_token: accessToken.value,
  refresh_token: refreshToken.value,
});

const { data: { user }, error } = await supabase.auth.getUser();

if (error || !user) {
  return Astro.redirect("/signin");
}
---

<BaseLayout>
  <div class="max-w-4xl mx-auto">
    <div class="bg-white rounded-lg shadow-lg border p-8">
      <div class="flex justify-between items-center mb-8 border-b pb-6">
        <div>
          <h1 class="text-4xl font-serif font-bold text-gray-900">Панель управления</h1>
          <p class="text-gray-600 mt-2">Добро пожаловать, <span class="font-medium">{user.email}</span>!</p>
        </div>
        <a 
          href="/api/signout" 
          class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-md transition duration-200"
        >
          Выйти
        </a>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="bg-blue-50 p-6 rounded-lg border border-blue-200">
          <h2 class="text-xl font-serif font-semibold text-blue-800 mb-2">📰 Новости</h2>
          <p class="text-blue-600">Читайте последние новости из разных категорий.</p>
          <a href="/" class="inline-block mt-3 text-blue-700 hover:text-blue-800 font-medium">Перейти к новостям →</a>
        </div>
        
        <div class="bg-green-50 p-6 rounded-lg border border-green-200">
          <h2 class="text-xl font-serif font-semibold text-green-800 mb-2">⚽ Спорт</h2>
          <p class="text-green-600">Следите за спортивными событиями и результатами.</p>
          <a href="/sport" class="inline-block mt-3 text-green-700 hover:text-green-800 font-medium">К спортивным новостям →</a>
        </div>
        
        <div class="bg-purple-50 p-6 rounded-lg border border-purple-200">
          <h2 class="text-xl font-serif font-semibold text-purple-800 mb-2">🎬 Развлечения</h2>
          <p class="text-purple-600">Узнавайте о последних событиях в мире развлечений.</p>
          <a href="/entertainment" class="inline-block mt-3 text-purple-700 hover:text-purple-800 font-medium">К развлекательным новостям →</a>
        </div>
      </div>
      
      <div class="bg-gray-50 p-6 rounded-lg border">
        <h2 class="text-2xl font-serif font-bold text-gray-900 mb-4">Информация об аккаунте</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm">
          <div>
            <strong class="text-gray-700">Email:</strong> 
            <span class="text-gray-900">{user.email}</span>
          </div>
          <div>
            <strong class="text-gray-700">Дата регистрации:</strong> 
            <span class="text-gray-900">{new Date(user.created_at).toLocaleDateString("ru-RU")}</span>
          </div>
          <div>
            <strong class="text-gray-700">Последний вход:</strong> 
            <span class="text-gray-900">{new Date(user.last_sign_in_at || user.created_at).toLocaleDateString("ru-RU")}</span>
          </div>
          <div>
            <strong class="text-gray-700">Статус:</strong> 
            <span class="text-green-600 font-medium">✓ Активен</span>
          </div>
        </div>
      </div>
      
      <div class="mt-8 text-center">
        <p class="text-gray-600">Теперь вы можете пользоваться всеми функциями MyNews!</p>
      </div>
    </div>
  </div>
</BaseLayout>